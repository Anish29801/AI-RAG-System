[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests that require a live Ollama instance (localhost:11434)
    api: marks tests that require a live FastAPI server (localhost:8000)
    e2e: marks tests that require both server and Ollama running
